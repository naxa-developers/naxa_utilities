version: '3.4'

services:
  covid:
    container_name: naxa_utilities
    image: mdillon/postgis:11-alpine
    ports:
     - "5432:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
    name: postgres-data
