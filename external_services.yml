version: '3.4'

services:
  covid:
    container_name: naxa_utilities
    image: mdillon/postgis:11-alpine
    volumes:
      - ./postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
    name: postgres-data
